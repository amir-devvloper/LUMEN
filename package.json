{
  "name": "lumen",
  "version": "1.0.0",
  "description": "LUMEN — music, focus, flow. Express + SQLite backend and static frontend.",
  "main": "backend/server.js",
"scripts": {
  "start": "node backend/server.js",
  "dev": "node --watch backend/server.js",
  "seed": "node database/seed.js",
  "start:seeded": "npm run seed && node backend/server.js",
  "test": "echo \"Error: no test specified\" && exit 1"
}
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "better-sqlite3": "^13.0.3",
    "cors": "^2.8.6",
    "express": "^5.2.1"
  }
}